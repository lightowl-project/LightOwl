[[inputs.bind]]
  urls = {{ urls }}

  {%- if gather_memory_contexts %}
  gather_memory_contexts = true
  {% endif %}

  timeout = '{{ timeout }}s'

  {%- if gather_views %}
  gather_views = true
  {% endif %}  